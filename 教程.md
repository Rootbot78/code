# GitHub 命令行操作完整教程

## 目录

1. [安装 Git](#1-安装-git)
2. [配置 Git](#2-配置-git)
3. [GitHub Token 创建](#3-github-token-创建)
4. [创建仓库](#4-创建仓库)
5. [克隆仓库](#5-克隆仓库)
6. [基本操作流程](#6-基本操作流程)
7. [文件操作](#7-文件操作)
8. [分支操作](#8-分支操作)
9. [查看状态和历史](#9-查看状态和历史)
10. [远程仓库操作](#10-远程仓库操作)
11. [删除仓库](#11-删除仓库)
12. [GitHub API 常用操作](#12-github-api-常用操作)
13. [GitHub Actions 自动构建](#13-github-actions-自动构建)
14. [常见问题](#14-常见问题)

---

## 1. 安装 Git

### Linux/Kali
```bash
sudo apt update && sudo apt install git
```

### macOS
```bash
brew install git
```

### Windows
1. 下载：https://git-scm.com/download/win
2. 运行安装程序
3. 勾选 "Git Bash here"

### 验证安装
```bash
git --version
```

---

## 2. 配置 Git

### 设置用户名和邮箱
```bash
git config --global user.name "你的GitHub用户名"
git config --global user.email "你的GitHub邮箱"
```

### 查看配置
```bash
git config --list
```

### 编辑配置文件
```bash
git config --global --edit
```

### 设置默认分支名
```bash
git config --global init.defaultBranch main
```

### 设置拉取策略
```bash
git config --global pull.rebase false  # 合并策略
git config --global pull.rebase true   # 变基策略
```

---

## 3. GitHub Token 创建

### 为什么需要 Token
GitHub 2021年后不再支持密码push，必须使用 Personal Access Token (PAT)。

### 创建步骤

1. 打开：https://github.com/settings/tokens
2. 点击 **Generate new token (Classic)**
3. 填写：
   - **Name**: 随便起名（如 `Git CLI`）
   - **Expiration**: 选择过期时间（建议 30-90 天）
   - **Scopes**: 勾选需要的权限

### 权限说明

| 权限 | 说明 |
|------|------|
| `repo` | 完全控制私有仓库 |
| `repo:status` | 读取仓库状态 |
| `workflow` | 管理 GitHub Actions |
| `delete_repo` | 删除仓库 |
| `read:user` | 读取用户信息 |

### 使用 Token 克隆
```bash
git clone https://用户名:Token@github.com/用户名/仓库名.git
```

示例：
```bash
git clone https://Rootbot78:ghp_xxxxxxxxxx@github.com/Rootbot78/code.git
```

---

## 4. 创建仓库

### 方法一：网页创建

1. 打开 https://github.com/new
2. 填写仓库名称
3. 选择公开/私有
4. 点击 Create repository

### 方法二：命令行创建（API）

```bash
# 创建新仓库
curl -s -u 用户名:Token -X POST \
  -H "Accept: application/vnd.github+json" \
  https://api.github.com/user/repos \
  -d '{"name":"仓库名","description":"描述","private":false}'

# 参数说明：
# private: true = 私有仓库，false = 公开仓库
```

### 方法三：本地初始化已有项目

```bash
# 初始化本地仓库
git init

# 添加远程仓库
git remote add origin https://用户名:Token@github.com/用户名/仓库名.git

# 添加文件
git add .
git commit -m "Initial commit"
git branch -M main
git push -u origin main
```

### 创建带 README 的仓库

```bash
# 先克隆空仓库
git clone https://用户名:Token@github.com/用户名/新仓库名.git
cd 新仓库名

# 创建文件
echo "# 项目名称" > README.md

# 提交推送
git add README.md
git commit -m "Add README"
git push
```

---

## 5. 克隆仓库

### 基本克隆
```bash
git clone https://github.com/用户名/仓库名.git
```

### 使用 Token 克隆
```bash
git clone https://用户名:Token@github.com/用户名/仓库名.git
```

### 克隆到指定目录
```bash
git clone https://github.com/用户名/仓库名.git 我的目录
```

### 浅克隆（只克隆最新版本）
```bash
git clone --depth 1 https://github.com/用户名/仓库名.git
```

### 克隆特定分支
```bash
git clone --branch 分支名 https://github.com/用户名/仓库名.git
git clone -b 分支名 https://github.com/用户名/仓库名.git
```

### 克隆所有分支
```bash
git clone --mirror https://github.com/用户名/仓库名.git
```

---

## 6. 基本操作流程

### 三步走：添加 → 提交 → 推送

```bash
# 1. 添加文件到暂存区
git add 文件名          # 添加单个文件
git add .              # 添加所有文件
git add -A             # 添加所有文件（包括删除）
git add -u             # 只添加已跟踪的文件

# 2. 提交更改
git commit -m "提交说明"

# 3. 推送到远程
git push
git push origin main   # 推送到 main 分支
git push -u origin main # 首次推送，关联分支
```

### 跳过暂存区直接提交
```bash
git commit -am "直接提交已跟踪的文件"
```

### 修改上次提交
```bash
git commit --amend -m "新说明"  # 修改提交信息
git commit --amend --no-edit    # 不修改提交信息，只添加新文件
```

### 查看暂存区
```bash
git status
git status -s  # 简洁模式
```

---

## 7. 文件操作

### 查看文件差异

```bash
git diff                    # 查看未暂存的修改
git diff --staged          # 查看已暂存的修改
git diff HEAD              # 查看所有修改（相对于上次提交）
git diff 分支1 分支2       # 比较两个分支
git diff commit1 commit2   # 比较两个提交
```

### 撤销操作

```bash
git checkout -- 文件名     # 撤销文件的未暂存修改
git restore 文件名         # 同上（新语法）
git restore --staged 文件名 # 取消暂存
git reset HEAD 文件名      # 取消暂存（旧语法）
```

### 删除文件

```bash
git rm 文件名              # 从git和工作区删除
git rm -f 文件名           # 强制删除
git rm --cached 文件名     # 从暂存区删除，但保留在工作区
```

### 重命名文件

```bash
git mv 旧文件名 新文件名   # 重命名文件
```

### 查看文件在某提交的内容

```bash
git show 提交ID:文件名
git show HEAD~1:文件名    # 查看上次提交的文件内容
```

---

## 8. 分支操作

### 查看分支

```bash
git branch                 # 查看本地分支
git branch -a              # 查看所有分支（包括远程）
git branch -r              # 查看远程分支
git branch -v              # 查看分支及最新提交
```

### 创建分支

```bash
git branch 分支名          # 创建分支
git checkout -b 分支名     # 创建并切换
git switch -c 分支名       # 创建并切换（新语法）
git branch 分支名 提交ID   # 基于某个提交创建分支
```

### 切换分支

```bash
git checkout 分支名        # 切换到分支
git switch 分支名          # 切换（新语法）
git checkout -            # 切换到上一个分支
```

### 删除分支

```bash
git branch -d 分支名       # 删除分支（已合并）
git branch -D 分支名       # 强制删除分支
git push origin --delete 分支名  # 删除远程分支
```

### 合并分支

```bash
git merge 分支名           # 合并分支到当前分支
git merge --no-ff 分支名   # 禁用快进合并
git merge --squash 分支名  # 压缩合并
```

### 重命名分支

```bash
git branch -m 旧名 新名    # 重命名当前分支
git branch -m 分支名 新名   # 重命名指定分支
```

### 推送分支

```bash
git push origin 分支名     # 推送分支到远程
git push -u origin 分支名  # 首次推送并关联
```

### 拉取远程分支

```bash
git pull                   # 拉取当前分支
git fetch origin           # 获取远程更新
git checkout -b 本地分支 origin/远程分支  # 跟踪远程分支
```

---

## 9. 查看状态和历史

### 查看状态

```bash
git status                 # 详细状态
git status -s              # 简洁状态
git status -sb             # 简洁状态并显示分支
```

### 查看提交历史

```bash
git log                    # 查看提交历史
git log --oneline          # 简洁历史
git log --graph           # 图形化历史
git log --graph --oneline  # 简洁图形化
git log -n 数量            # 查看最近N条
git log --author 用户名    # 筛选某用户的提交
git log --since 日期       # 筛选某日期后的提交
git log --until 日期       # 筛选某日期前的提交
git log --grep 关键词      # 筛选提交信息含关键词的
git log -p 文件名          # 查看某个文件的提交历史
git log --stat             # 显示文件变更统计
```

### 查看谁修改了某行

```bash
git blame 文件名           # 查看文件每行的修改者
git blame -L 10,20 文件名  # 查看指定行数
```

### 查看暂存区

```bash
git ls-files               # 查看暂存区的文件
```

---

## 10. 远程仓库操作

### 查看远程仓库

```bash
git remote -v              # 查看远程仓库地址
git remote show origin    # 查看远程仓库详细信息
```

### 添加远程仓库

```bash
git remote add 名称 URL    # 添加远程仓库
git remote add origin https://github.com/用户/仓库.git
```

### 修改远程仓库

```bash
git remote set-url origin 新URL    # 修改远程仓库地址
git remote set-url --push origin 新URL  # 只修改推送地址
```

### 重命名远程仓库

```bash
git remote rename 原名 新名
```

### 删除远程仓库

```bash
git remote remove 名称
```

### 更新远程分支列表

```bash
git remote update
git fetch --all
```

---

## 11. 删除仓库

### 方法一：网页删除

1. 进入仓库页面
2. 点击 **Settings**
3. 滚动到最下方
4. 点击 **Delete this repository**
5. 输入仓库名确认

### 方法二：API 删除

```bash
# 删除仓库
curl -s -u 用户名:Token -X DELETE \
  -H "Accept: application/vnd.github+json" \
  https://api.github.com/repos/用户名/仓库名
```

### 危险操作警告

⚠️ 删除仓库后，所有数据将无法恢复！

---

## 12. GitHub API 常用操作

### 设置变量（方便复用）

```bash
TOKEN="your_token"
USER="your_username"
REPO="repo_name"
```

### 用户操作

```bash
# 查看用户信息
curl -s -u 用户名:Token https://api.github.com/user

# 查看指定用户信息
curl -s https://api.github.com/users/用户名
```

### 仓库操作

```bash
# 创建仓库
curl -s -u 用户名:Token -X POST \
  -H "Accept: application/vnd.github+json" \
  https://api.github.com/user/repos \
  -d '{"name":"仓库名","private":false}'

# 查看仓库信息
curl -s -u 用户名:Token https://api.github.com/repos/用户名/仓库名

# 更新仓库描述
curl -s -u 用户名:Token -X PATCH \
  -H "Accept: application/vnd.github+json" \
  https://api.github.com/repos/用户名/仓库名 \
  -d '{"name":"新名称","description":"新描述"}'

# 删除仓库
curl -s -u 用户名:Token -X DELETE \
  -H "Accept: application/vnd.github+json" \
  https://api.github.com/repos/用户名/仓库名

# 列出用户所有仓库
curl -s -u 用户名:Token https://api.github.com/user/repos?type=all&sort=updated

# 列出仓库内容
curl -s -u 用户名:Token https://api.github.com/repos/用户名/仓库名/contents/
```

### 文件操作

```bash
# 上传文件（创建文件）
curl -s -u 用户名:Token -X PUT \
  -H "Accept: application/vnd.github+json" \
  https://api.github.com/repos/用户名/仓库名/contents/路径/文件名 \
  -d '{"message":"提交信息","content":"文件base64内容","branch":"main"}'

# 读取文件
curl -s -u 用户名:Token https://api.github.com/repos/用户名/仓库名/contents/文件名

# 删除文件
curl -s -u 用户名:Token -X DELETE \
  -H "Accept: application/vnd.github+json" \
  https://api.github.com/repos/用户名/仓库名/contents/文件名 \
  -d '{"message":"删除文件","sha":"文件SHA","branch":"main"}'
```

### 获取文件 SHA

```bash
curl -s -u 用户名:Token \
  https://api.github.com/repos/用户名/仓库名/contents/文件名 | \
  jq -r '.sha'
```

### Base64 编码文件

```bash
# Linux
CONTENT=$(base64 -w 0 文件名)

# macOS
CONTENT=$(base64 文件名)

# 上传
curl -s -u 用户名:Token -X PUT \
  -H "Accept: application/vnd.github+json" \
  https://api.github.com/repos/用户名/仓库名/contents/文件名 \
  -d "{\"message\":\"Add file\",\"content\":\"$CONTENT\",\"branch\":\"main\"}"
```

### GitHub Actions 操作

```bash
# 查看 workflows
curl -s -u 用户名:Token https://api.github.com/repos/用户名/仓库名/actions/workflows

# 触发 workflow
curl -s -u 用户名:Token -X POST \
  -H "Accept: application/vnd.github+json" \
  https://api.github.com/repos/用户名/仓库名/actions/workflows/ID/dispatches \
  -d '{"ref":"main","inputs":{"entry_file":"main.py"}}'

# 查看 workflow runs
curl -s -u 用户名:Token https://api.github.com/repos/用户名/仓库名/actions/runs

# 查看 run 详情
curl -s -u 用户名:Token https://api.github.com/repos/用户名/仓库名/actions/runs/运行ID

# 取消 workflow
curl -s -u 用户名:Token -X POST \
  -H "Accept: application/vnd.github+json" \
  https://api.github.com/repos/用户名/仓库名/actions/runs/运行ID/cancel
```

### 分支操作

```bash
# 列出分支
curl -s -u 用户名:Token https://api.github.com/repos/用户名/仓库名/branches

# 创建分支
curl -s -u 用户名:Token -X POST \
  -H "Accept: application/vnd.github+json" \
  https://api.github.com/repos/用户名/仓库名/git/refs \
  -d '{"ref":"refs/heads/新分支","sha":"基础分支的SHA"}'

# 删除分支
curl -s -u 用户名:Token -X DELETE \
  -H "Accept: application/vnd.github+json" \
  https://api.github.com/repos/用户名/仓库名/git/refs/heads/分支名
```

### Issues 操作

```bash
# 列出 issues
curl -s -u 用户名:Token https://api.github.com/repos/用户名/仓库名/issues

# 创建 issue
curl -s -u 用户名:Token -X POST \
  -H "Accept: application/vnd.github+json" \
  https://api.github.com/repos/用户名/仓库名/issues \
  -d '{"title":"标题","body":"内容"}'

# 关闭 issue
curl -s -u 用户名:Token -X PATCH \
  -H "Accept: application/vnd.github+json" \
  https://api.github.com/repos/用户名/仓库名/issues/编号 \
  -d '{"state":"closed"}'
```

---

## 13. GitHub Actions 自动构建

### 创建 workflow 目录

```bash
mkdir -p .github/workflows
```

### 编写 workflow 文件

示例：Python 打包成 exe

```yaml
name: Build EXE

on:
  workflow_dispatch:
    inputs:
      entry_file:
        description: 'Python 入口文件名'
        required: true
        default: 'main.py'

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        if (Test-Path requirements.txt) { pip install -r requirements.txt }

    - name: Install PyInstaller
      run: pip install pyinstaller

    - name: Build EXE
      run: pyinstaller --onefile --name MyApp ${{ github.event.inputs.entry_file }}

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: ${{ github.event.inputs.entry_file }}-exe
        path: dist/MyApp.exe
```

### 推送 workflow

```bash
git add .
git commit -m "Add build workflow"
git push
```

### 查看 workflow 运行状态

```bash
# 使用 API 查看
curl -s -u 用户名:Token \
  https://api.github.com/repos/用户名/仓库名/actions/runs | jq '.workflow_runs[] | {name, status, conclusion}'
```

### 下载 artifact

```bash
# 获取 artifact 下载链接
curl -s -u 用户名:Token \
  https://api.github.com/repos/用户名/仓库名/actions/runs/运行ID/artifacts | jq '.artifacts[0].archive_download_url'
```

---

## 14. 常见问题

### Q: 推送时报 403 Forbidden
A: Token 无效或权限不足，重新生成 Token。

### Q: 提示 "Please tell me who you are"
A: 需要设置 Git 用户名和邮箱：
```bash
git config --global user.name "用户名"
git config --global user.email "邮箱"
```

### Q: 如何更新 Token？
```bash
git remote set-url origin https://用户名:新Token@github.com/用户名/仓库名.git
git push
```

### Q: 如何查看远程仓库地址？
```bash
git remote -v
```

### Q: 克隆失败怎么办？
A: 检查 Token 权限，确保有 `repo` 权限。

### Q: 如何放弃本地修改？
```bash
git checkout -- .
git reset --hard HEAD
```

### Q: 如何查看某个文件的历史？
```bash
git log -p 文件名
```

### Q: 如何查看远程有什么分支？
```bash
git fetch
git branch -a
```

### Q: 误删分支如何恢复？
```bash
# 找到丢失的提交
git reflog
# 然后创建分支
git branch 分支名 提交ID
```

### Q: 如何查看仓库大小？
A: 打开 https://github.com/用户名/仓库名/settings 页面最下方。

---

## 快速参考表

| 操作 | 命令 |
|------|------|
| 克隆仓库 | `git clone URL` |
| 添加文件 | `git add .` |
| 提交 | `git commit -m "说明"` |
| 推送 | `git push` |
| 拉取 | `git pull` |
| 查看状态 | `git status` |
| 查看日志 | `git log --oneline` |
| 创建分支 | `git branch 名` |
| 切换分支 | `git checkout 名` |
| 合并分支 | `git merge 名` |
| 查看远程 | `git remote -v` |
| 修改远程 | `git remote set-url origin URL` |
| 创建仓库 | `curl -X POST ... /user/repos` |
| 删除仓库 | `curl -X DELETE ... /repos/用户/仓库` |
| 查看分支 | `git branch -a` |
| 推送分支 | `git push -u origin 分支` |
| 删除本地分支 | `git branch -d 名` |
| 删除远程分支 | `git push origin --delete 名` |
| 撤销修改 | `git checkout -- 文件` |
| 取消暂存 | `git reset HEAD 文件` |
| 查看差异 | `git diff` |
| 查看某行作者 | `git blame 文件` |
| 创建 Tag | `git tag v1.0` |
| 推送 Tag | `git push --tags` |
| 查看 Tag | `git tag` |