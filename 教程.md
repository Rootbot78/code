# GitHub 命令行操作教程

## 1. 安装 Git

### Linux/Kali
```bash
sudo apt update && sudo apt install git
```

### Windows
从 https://git-scm.com 下载安装

---

## 2. 配置 Git

```bash
git config --global user.name "你的用户名"
git config --global user.email "你的邮箱"
```

---

## 3. 克隆仓库

### HTTPS 方式（需 Token）
```bash
git clone https://github.com/用户名/仓库名.git
```

### 使用 Token 克隆
```bash
git clone https://用户名:Token@github.com/用户名/仓库名.git
```

示例：
```bash
git clone https://Rootbot78:ghp_xxxxxxxxxx@github.com/Rootbot78/code.git
```

---

## 4. 基本操作流程

### 添加文件到暂存区
```bash
git add 文件名          # 添加单个文件
git add .              # 添加所有文件
```

### 提交更改
```bash
git commit -m "提交说明"
```

### 推送到远程
```bash
git push               # 推送到当前分支
git push origin main   # 推送到 main 分支
```

### 拉取更新
```bash
git pull               # 拉取并合并
```

---

## 5. 分支操作

```bash
git branch                    # 查看分支
git branch 新分支名           # 创建分支
git checkout 分支名          # 切换分支
git checkout -b 分支名        # 创建并切换
git merge 分支名             # 合并分支
```

---

## 6. 查看状态

```bash
git status        # 查看工作区状态
git diff          # 查看未暂存的修改
git log           # 查看提交历史
```

---

## 7. GitHub Actions 自动构建

### 创建工作流目录
```bash
mkdir -p .github/workflows
```

### 推送 workflow
```bash
git add .
git commit -m "Add workflow"
git push
```

---

## 8. 使用 GitHub API

### 查看仓库信息
```bash
curl -s -u 用户名:Token https://api.github.com/repos/用户名/仓库名
```

### 查看 workflows
```bash
curl -s -u 用户名:Token https://api.github.com/repos/用户名/仓库名/actions/workflows
```

### 触发 workflow
```bash
curl -s -u 用户名:Token -X POST \
  -H "Accept: application/vnd.github+json" \
  https://api.github.com/repos/用户名/仓库名/actions/workflows/ID/dispatches \
  -d '{"ref":"main","inputs":{"entry_file":"main.py"}}'
```

---

## 常见问题

### Q: 推送时提示权限不足
A: Token 缺少权限，需要在 GitHub 创建带有 `repo` 和 `workflow` 权限的 Token

### Q: 如何更新 Token
A: 编辑 .git/config 或重新克隆

### Q: 如何查看远程仓库地址
```bash
git remote -v
```

---

## 快速参考

| 操作 | 命令 |
|------|------|
| 克隆 | `git clone URL` |
| 添加 | `git add .` |
| 提交 | `git commit -m "说明"` |
| 推送 | `git push` |
| 拉取 | `git pull` |
| 状态 | `git status` |
| 日志 | `git log --oneline` |